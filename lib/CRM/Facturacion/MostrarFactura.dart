import 'package:Balewaterproject/model/Record.dart';
import 'package:flutter/material.dart';

class MostrarFactura extends StatelessWidget {
  Record record;
  MostrarFactura({ Key key, this.record}): super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
